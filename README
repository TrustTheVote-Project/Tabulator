#################
# Ruby Programs #
#################

operator.rb
- Operates the Tabulator
- Prints help info when called without args
- Reads and Writes the file TABULATOR_COUNT.yml

lib/tabulator.rb
- The Tabulator

lib/validate.rb
- The Validator for Tabulator data sets

lib/check_syntax_yaml.rb
- YAML Syntax Checker for TTV Common Data Format (CDF) datasets

test/operator_test.rb
- Ruby Unit Tests for the Tabulator Operator

test/tabulator_test.rb
- Ruby Unit Tests for the Tabulator

test/validator_test.rb
- Ruby Unit Tests for the Tabulator Validator

test/check_syntax_yaml_test.rb
- Setup and Test the YAML Syntax Checker

test/emgr_data_handler.rb
- [DEBUG] Converts two real EMGR datasets into an Election Definition in
  preparation for syntax checking and validation by the Tabulator

###########################
# Ruby Unit Test Commands #
###########################

ruby -I . test/operator_test.rb
ruby -I . test/tabulator_test.rb
ruby -I . test/validator_test.rb
ruby -I . test/check_syntax_yaml_test.rb

################
# Bash Scripts #
################

TEST.sh
- Valid arguments: all, syn(tax), val(idator), tab(ulator), op(erator), def(ault)
- <nothing>: run all of the Ruby unit tests
- all : run all of the tests
- syn: run all of the Tabulator syntax tests
- val: run all of the Tabulator Validator tests
- tab: run all of the Tabulator Vote Counting tests
- op: run all of the Tabulator Operator tests
- def: run a default set of tests using a prototypical Jurisdiction
       Definition, Election Definition, and three Counter Counts, achieving
       the Tabulator DONE state  

###############
# Directories #
###############

data/
- Docs/
  - miscellaneous documents
- Schemas/
  - schema files for all data structures processed by the Tabulator
- Tests/
  - test data files

doc/
- RDoc documentation

lib/
- primary Ruby executables

test/
- Ruby unit tests

-----------------
Author: Jeff Cook
Date: 18 Feb 2011
