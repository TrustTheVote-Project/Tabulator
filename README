#################
# Ruby Programs #
#################

operator.rb
- Operates the Tabulator
- Prints help info when called without args
- Reads and Writes the file TABULATOR_COUNT.yml

lib/tabulator.rb
- The Tabulator

lib/validate.rb
- The Validator for Tabulator data sets

lib/check_syntax_yaml.rb
- YAML Syntax Checker for TTV Common Data Format (CDF) datasets

test/operator_test.rb
- Ruby Unit Tests for the Tabulator Operator

test/tabulator_test.rb
- Ruby Unit Tests for the Tabulator and Validator

test/check_syntax_yaml_test.rb
- Setup and Test the YAML Syntax Checker

test/emgr_data_handler.rb
- [DEBUG] Converts two real EMGR datasets into an Election Definition in
  preparation for syntax checking and validation by the Tabulator

###########################
# Ruby Unit Test Commands #
###########################

ruby -I . test/operator_test.rb"
ruby -I . test/tabulator_test.rb"
ruby -I . test/check_syntax_yaml_test.rb"

################
# Bash Scripts #
################

TEST.sh
- Valid arguments: all, syn(tax), tab(ulator), op(erator), def(ault)
- <nothing>: run all of the Ruby unit tests
- all : run all of the tests
- default: run the default tests using a prototypical Jurisdiction
           Definition, Election Definition, and three Counter Counts,
	   achieving the Tabulator DONE state 
- syntax: run all of the Tabulator syntax tests
- tab: run all of the Tabulator validation tests
- op: run all of the Tabulator Operator tests

###############
# Directories #
###############

Docs/
- the BNF for the language recognized by the Tabulator
- a list of Tabulator ERRORS
- the Tabulator SPEC

Schemas/
- schema files encompassing all data structures processed by the Tabulator

Tests/
- test data files (Syntax, Election Definitions, Counter Counts, EMGR datasets)

-----------------
Author: Jeff Cook
Date: 2 Feb 2011
